tunnel: REPLACE_WITH_TUNNEL_UUID
credentials-file: REPLACE_WITH_CLOUDFLARED_CREDENTIALS_FILE

ingress:
  - hostname: app.your-domain.example
    service: http://HEGEL_ORIGIN_HOST:HEGEL_ORIGIN_PORT
  - hostname: admin.your-domain.example
    service: http://HEGEL_ORIGIN_HOST:HEGEL_ORIGIN_PORT
  - service: http_status:404
